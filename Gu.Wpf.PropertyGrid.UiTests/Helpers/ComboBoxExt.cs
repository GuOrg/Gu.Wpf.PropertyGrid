namespace Gu.Wpf.PropertyGrid.UiTests
{
    using System;
    using System.Windows.Automation;

    using TestStack.White.UIItems;
    using TestStack.White.UIItems.ListBoxItems;

    public static class ComboBoxExt
    {
        //public static bool IsReadOnly(this ComboBox comboBox)
        //{
        //    object patternObject;
        //    if (comboBox.AutomationElement.TryGetCurrentPattern(ValuePattern.Pattern, out patternObject))
        //    {
        //        var basePattern = (ValuePattern)patternObject;
        //        return basePattern.Current.IsReadOnly;
        //    }

        //    throw new InvalidOperationException("Could not get pattern.");
        //}
    }
}