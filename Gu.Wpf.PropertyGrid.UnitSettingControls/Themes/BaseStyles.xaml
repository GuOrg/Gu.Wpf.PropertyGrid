<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:propertyGrid="http://gu.se/PropertyGrid"
                    xmlns:numeric="http://gu.se/NumericInput"
                    xmlns:propertyGrid1="clr-namespace:Gu.Wpf.PropertyGrid">

    <Style x:Key="{x:Static propertyGrid:Keys.SettingRowUnitValueStyleKey}"
           TargetType="{x:Type numeric:DoubleBox}" />

    <Style x:Key="{x:Static propertyGrid:Keys.UnitSettingBaseStyleKey}"
           TargetType="{x:Type propertyGrid:SettingControlBase}">
        <Setter Property="HeaderStyle" Value="{DynamicResource {x:Static propertyGrid:Keys.SettingRowHeaderStyleKey}}" />
        <Setter Property="SuffixStyle" Value="{DynamicResource {x:Static propertyGrid:Keys.SettingRowSuffixStyleKey}}" />
        <Setter Property="OldValue" Value="{DynamicResource {x:Static propertyGrid:Keys.SettingRowSuffixStyleKey}}" />
        <Setter Property="ValueStyle" Value="{DynamicResource {x:Static propertyGrid:Keys.SettingRowUnitValueStyleKey}}" />
        <Setter Property="ErrorStyle" Value="{DynamicResource {x:Static propertyGrid:Keys.SettingRowSuffixStyleKey}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type propertyGrid:SettingControlBase}">
                    <Grid Margin="{TemplateBinding Padding}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"
                                              SharedSizeGroup="{x:Static propertyGrid:SharedSizeGroups.LabelColumn}" />
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{TemplateBinding ValueMinWidth}"
                                              SharedSizeGroup="{x:Static propertyGrid:SharedSizeGroups.ValueColumn}" />
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{TemplateBinding SuffixMinWidth}"
                                              SharedSizeGroup="{x:Static propertyGrid:SharedSizeGroups.SuffixColumn}" />
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="Header"
                                   Grid.Row="0"
                                   Grid.Column="0"
                                   Style="{TemplateBinding HeaderStyle}"
                                   Text="{TemplateBinding Header}" />

                        <numeric:DoubleBox x:Name="ValueBox"
                                           Grid.Row="0"
                                           Grid.Column="1"
                                           MaxValue="{propertyGrid1:QuantityBinding MaxValue}"
                                           MinValue="{propertyGrid1:QuantityBinding MinValue}"
                                           Style="{TemplateBinding ValueStyle}"
                                           Value="{propertyGrid1:QuantityBinding Value}" />

                        <TextBlock x:Name="SuffixBox"
                                   Grid.Row="0"
                                   Grid.Column="2"
                                   Style="{TemplateBinding SuffixStyle}"
                                   Text="{TemplateBinding Suffix}" />

                        <!--<Grid x:Name="InfoGrid"
                              Grid.Row="1"
                              Grid.Column="0"
                              Grid.ColumnSpan="2"
                              HorizontalAlignment="Right"
                              TextElement.FontSize="12">
                            <TextBlock HorizontalAlignment="Right"
                                       Foreground="LightGray"
                                       Text="missing"
                                       Visibility="{Binding Path=IsDirty,
                                                            RelativeSource={RelativeSource TemplatedParent},
                                                            Converter={propertyGrid:BooleanToVisibilityConverter WhenTrue=Collapsed,
                                                                                                                 WhenNull=Visible,
                                                                                                                 WhenFalse=Collapsed}}" />

                        -->
                        <!--<TextBlock x:Name="ErrorTextBox"
                                       HorizontalAlignment="Right"
                                       DataContext="{Binding ElementName=ValueBox}"
                                       Foreground="Red"
                                       Text="error"
                                       Visibility="{Binding Path=(Validation.HasError),
                                                            ElementName=ValueBox,
                                                            Converter={propertyGrid:BooleanToVisibilityConverter WhenTrue=Visible,
                                                                                                                 WhenNull=Collapsed,
                                                                                                                 WhenFalse=Collapsed}}" />-->
                        <!--

                            <TextBlock x:Name="OldValueBox"
                                       HorizontalAlignment="Right"
                                       Style="{TemplateBinding OldValueStyle}"
                                       Visibility="{TemplateBinding IsDirty,
                                                                    Converter={propertyGrid:BooleanToVisibilityConverter WhenTrue=Visible,
                                                                                                                         WhenNull=Hidden,
                                                                                                                         WhenFalse=Hidden}}">
                                <Run Text="OldValue: " />
                                <Run Text="{TemplateBinding OldValue}" />
                            </TextBlock>
                        </Grid>-->
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>