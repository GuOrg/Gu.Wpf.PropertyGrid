<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:l="clr-namespace:Gu.Wpf.PropertyGrid"
                    xmlns:numeric="http://gu.se/NumericInput">

    <Style x:Key="{x:Static l:Keys.SettingRowBaseStyle}" TargetType="{x:Type Control}">
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Padding" Value="0,12" />
    </Style>

    <Style x:Key="HeaderedContentControlSettingStyle"
           BasedOn="{StaticResource {x:Static l:Keys.SettingRowBaseStyle}}"
           TargetType="{x:Type HeaderedContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type HeaderedContentControl}">
                    <Grid Margin="{TemplateBinding Padding}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="{x:Static l:SharedSizeGroups.LabelColumn}" />
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{Binding Path=(l:SettingControl.ValueWidth),
                                                                 RelativeSource={RelativeSource TemplatedParent}}"
                                              SharedSizeGroup="{x:Static l:SharedSizeGroups.ValueColumn}" />
                            <ColumnDefinition Width="Auto"
                                              MinWidth="{Binding Path=(l:SettingControl.SuffixWidth),
                                                                 RelativeSource={RelativeSource TemplatedParent}}"
                                              SharedSizeGroup="{x:Static l:SharedSizeGroups.SuffixColumn}" />
                        </Grid.ColumnDefinitions>

                        <TextBlock x:Name="Header"
                                   Grid.Row="0"
                                   Grid.Column="0"
                                   Style="{StaticResource SettingRowHeaderStyle}"
                                   Text="{TemplateBinding Header}" />

                        <ContentPresenter x:Name="ValuePresenter"
                                          Grid.Row="0"
                                          Grid.Column="1"
                                          ContentSource="Content" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="SettingRowTemplate">
        <Grid Margin="{TemplateBinding Padding}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="{x:Static l:SharedSizeGroups.LabelColumn}" />
                <ColumnDefinition Width="Auto"
                                  MinWidth="{Binding Path=(l:SettingControl.ValueWidth),
                                                     RelativeSource={RelativeSource TemplatedParent}}"
                                  SharedSizeGroup="{x:Static l:SharedSizeGroups.ValueColumn}" />
                <ColumnDefinition Width="Auto"
                                  MinWidth="{Binding Path=(l:SettingControl.SuffixWidth),
                                                     RelativeSource={RelativeSource TemplatedParent}}"
                                  SharedSizeGroup="{x:Static l:SharedSizeGroups.SuffixColumn}" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="Header"
                       Grid.Row="0"
                       Grid.Column="0"
                       Style="{StaticResource {x:Static l:Keys.SettingRowHeaderStyleKey}}"
                       Text="{Binding Header,
                                      RelativeSource={RelativeSource TemplatedParent}}" />

            <ContentPresenter x:Name="ValuePresenter"
                              Grid.Row="0"
                              Grid.Column="1"
                              ContentTemplate="{Binding ValueTemplate,
                                                        RelativeSource={RelativeSource TemplatedParent}}"
                              numeric:NumericBox.DecimalDigits="{Binding Path=(numeric:NumericBox.DecimalDigits),
                                                                         RelativeSource={RelativeSource TemplatedParent}}" />

            <TextBlock x:Name="SuffixBox"
                       Grid.Row="0"
                       Grid.Column="2"
                       Style="{StaticResource {x:Static l:Keys.SettingRowSuffixStyle}}"
                       Text="{Binding Suffix,
                                      RelativeSource={RelativeSource TemplatedParent}}" />

            <Grid x:Name="InfoGrid"
                  Grid.Row="1"
                  Grid.Column="0"
                  Grid.ColumnSpan="2"
                  HorizontalAlignment="Right"
                  TextElement.FontSize="12">
                <TextBlock HorizontalAlignment="Right"
                           Foreground="LightGray"
                           Text="missing"
                           Visibility="{Binding Path=IsDirty,
                                                RelativeSource={RelativeSource AncestorType={x:Type l:SettingControlBase}},
                                                Converter={l:BooleanToVisibilityConverter WhenTrue=Collapsed,
                                                                                          WhenNull=Visible,
                                                                                          WhenFalse=Collapsed}}" />

                <!--<TextBlock x:Name="ErrorTextBox"
                           DataContext="{Binding ElementName=ValueBox}"
                           HorizontalAlignment="Right"
                           Foreground="Red"
                           Text="error"
                           Visibility="{Binding HasError,
                                                Converter={mui:BooleanToVisibilityConverter WhenTrue=Visible,
                                                                                            WhenNull=Collapsed,
                                                                                            WhenFalse=Collapsed}}" />-->

                <TextBlock x:Name="OldValueBox"
                           HorizontalAlignment="Right"
                           Foreground="{DynamicResource Accent}"
                           Visibility="{Binding Path=IsDirty,
                                                RelativeSource={RelativeSource AncestorType={x:Type l:SettingControlBase}},
                                                Converter={l:BooleanToVisibilityConverter WhenTrue=Visible,
                                                                                          WhenNull=Hidden,
                                                                                          WhenFalse=Hidden}}">
                    <Run Text="OldValue: " />
                    <Run Text="{Binding OldValue, RelativeSource={RelativeSource TemplatedParent}}" />
                </TextBlock>
            </Grid>
        </Grid>
    </ControlTemplate>

    <!--<Style BasedOn="{StaticResource SettingRowBaseStyle}" TargetType="{x:Type l:DoubleSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Setter Property="ValueTemplate">
            <Setter.Value>
                <DataTemplate>
                    <numeric:DoubleBox x:Name="ValueBox"
                                       MaxValue="{Binding MaxValue,
                                                          RelativeSource={RelativeSource AncestorType={x:Type l:DoubleSettingControl}}}"
                                       MinValue="{Binding MinValue,
                                                          RelativeSource={RelativeSource AncestorType={x:Type l:DoubleSettingControl}}}"
                                       Value="{Binding Value,
                                                       RelativeSource={RelativeSource AncestorType={x:Type l:DoubleSettingControl}}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <!--<Style BasedOn="{StaticResource SettingRowBaseStyle}" TargetType="{x:Type l:IntSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Setter Property="OldStringValue" Value="{Binding OldValue, RelativeSource={RelativeSource Self}}" />
        <Setter Property="ValueTemplate">
            <Setter.Value>
                <DataTemplate>
                    <numeric:IntBox x:Name="ValueBox"
                                    MaxValue="{Binding MaxValue,
                                                       RelativeSource={RelativeSource AncestorType={x:Type l:IntSettingControl}}}"
                                    MinValue="{Binding MinValue,
                                                       RelativeSource={RelativeSource AncestorType={x:Type l:IntSettingControl}}}"
                                    Value="{Binding Value,
                                                    RelativeSource={RelativeSource AncestorType={x:Type l:IntSettingControl}}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="OldValueTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Style="{StaticResource SettingOldValueStyle}" Text="{Binding OldValue, RelativeSource={RelativeSource AncestorType={x:Type l:IntSettingControl}}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SettingRowBaseStyle}" TargetType="{x:Type l:BoolSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Setter Property="OldStringValue" Value="{Binding OldValue, RelativeSource={RelativeSource Self}}" />
        <Setter Property="ValueTemplate">
            <Setter.Value>
                <DataTemplate>
    -->
    <!--<toggleSwitch:HorizontalToggleSwitch x:Name="ValueBox"
                                                         IsChecked="{Binding Value,
                                                                             RelativeSource={RelativeSource AncestorType={x:Type views:BoolSettingControl}}}"
                                                         Style="{StaticResource ModernUIToggleSwitch}" />-->
    <!--
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource SettingRowBaseStyle}" TargetType="{x:Type l:StringSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Setter Property="OldStringValue" Value="{Binding OldValue, RelativeSource={RelativeSource Self}}" />
        <Setter Property="ValueTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBox x:Name="ValueBox" Text="{Binding Value, RelativeSource={RelativeSource AncestorType={x:Type l:IntSettingControl}}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->

    <!--<units:LengthConverter x:Key="MillimetresConverter"
                         Unit="{x:Static units:LengthUnit.Millimetres}" />

    <units:LengthConverter x:Key="MicrometresConverter"
                         Unit="{x:Static units:LengthUnit.Micrometres}" />

    <Style BasedOn="{StaticResource SettingRowBaseStyle}"
           TargetType="{x:Type views:LengthSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Style.Triggers>
            <Trigger Property="Unit" Value="{x:Static unit:LengthUnit.Millimetres}">
                <Setter Property="ValueTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <numeric:DoubleBox x:Name="ValueBox"
                                                    MaxValue="{Binding MaxValue,
                                                                       Converter={StaticResource MillimetresConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}"
                                                    MinValue="{Binding MinValue,
                                                                       Converter={StaticResource MillimetresConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}"
                                                    Value="{Binding Value,
                                                                    Converter={StaticResource MillimetresConverter},
                                                                    RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="OldStringValue" Value="{Binding OldValue, Converter={units:LengthConverter Unit={x:Static units:LengthUnit.Millimetres}, UnitInput=SymbolRequired}, RelativeSource={RelativeSource Self}}" />
            </Trigger>

            <Trigger Property="Unit" Value="{x:Static unit:LengthUnit.Micrometres}">
                <Setter Property="ValueTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <numeric:DoubleBox x:Name="ValueBox"
                                                    MaxValue="{Binding MaxValue,
                                                                       Converter={StaticResource MicrometresConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}"
                                                    MinValue="{Binding MinValue,
                                                                       Converter={StaticResource MicrometresConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}"
                                                    Value="{Binding Value,
                                                                    Converter={StaticResource MicrometresConverter},
                                                                    RelativeSource={RelativeSource AncestorType={x:Type views:LengthSettingControl}}}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="OldStringValue" Value="{Binding OldValue, Converter={units:LengthConverter Unit={x:Static unit:LengthUnit.Micrometres}, UnitInput=SymbolRequired}, RelativeSource={RelativeSource Self}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <units:AngleConverter x:Key="DegreesConverter"
                        Unit="{x:Static unit:AngleUnit.Degrees}" />
    <units:AngleConverter x:Key="RadiansConverter"
                        Unit="{x:Static unit:AngleUnit.Radians}" />

    <Style BasedOn="{StaticResource SettingRowBaseStyle}"
           TargetType="{x:Type views:AngleSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />

        <Style.Triggers>
            <Trigger Property="Unit" Value="{x:Static unit:AngleUnit.Degrees}">
                <Setter Property="ValueTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <numeric:DoubleBox x:Name="ValueBox"
                                                    MaxValue="{Binding MaxValue,
                                                                       Converter={StaticResource DegreesConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}"
                                                    MinValue="{Binding MinValue,
                                                                       Converter={StaticResource DegreesConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}"
                                                    Value="{Binding Value,
                                                                    Converter={StaticResource DegreesConverter},
                                                                    RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="OldStringValue" Value="{Binding OldValue, Converter={units:AngleConverter Unit={x:Static units:AngleUnit.Degrees}, UnitInput=SymbolRequired}, RelativeSource={RelativeSource Self}}" />
            </Trigger>

            <Trigger Property="Unit" Value="{x:Static unit:AngleUnit.Radians}">
                <Setter Property="ValueTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <numeric:DoubleBox x:Name="ValueBox"
                                                    MaxValue="{Binding MaxValue,
                                                                       Converter={StaticResource RadiansConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}"
                                                    MinValue="{Binding MinValue,
                                                                       Converter={StaticResource RadiansConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}"
                                                    Value="{Binding Value,
                                                                    Converter={StaticResource RadiansConverter},
                                                                    RelativeSource={RelativeSource AncestorType={x:Type views:AngleSettingControl}}}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="OldStringValue" Value="{Binding OldValue, Converter={units:AngleConverter Unit={x:Static units:AngleUnit.Radians}, UnitInput=SymbolRequired}, RelativeSource={RelativeSource Self}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <units:PressureConverter x:Key="MegapascalsConverter"
                           Unit="{x:Static units:PressureUnit.Megapascals}" />

    <Style BasedOn="{StaticResource SettingRowBaseStyle}"
           TargetType="{x:Type views:PressureSettingControl}">
        <Setter Property="Template" Value="{StaticResource SettingRowTemplate}" />
        <Style.Triggers>
            <Trigger Property="Unit" Value="{x:Static units:PressureUnit.Megapascals}">
                <Setter Property="ValueTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <numericInput:DoubleBox x:Name="ValueBox"
                                                    MaxValue="{Binding MaxValue,
                                                                       Converter={StaticResource MegapascalsConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:PressureSettingControl}}}"
                                                    MinValue="{Binding MinValue,
                                                                       Converter={StaticResource MegapascalsConverter},
                                                                       RelativeSource={RelativeSource AncestorType={x:Type views:PressureSettingControl}}}"
                                                    Value="{Binding Value,
                                                                    Converter={StaticResource MegapascalsConverter},
                                                                    RelativeSource={RelativeSource AncestorType={x:Type views:PressureSettingControl}}}" />
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="OldStringValue" Value="{Binding OldValue, Converter={units:PressureConverter Unit={x:Static units:PressureUnit.Megapascals}, UnitInput=SymbolRequired}, RelativeSource={RelativeSource Self}}" />
            </Trigger>
        </Style.Triggers>
    </Style>-->
</ResourceDictionary>