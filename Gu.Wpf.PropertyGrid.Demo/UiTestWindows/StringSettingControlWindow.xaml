<Window x:Class="Gu.Wpf.PropertyGrid.Demo.UiTestWindows.StringSettingControlWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:demo="clr-namespace:Gu.Wpf.PropertyGrid.Demo"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:propertyGrid="http://gu.se/PropertyGrid"
        Title="StringSettingControlWindow"
        SizeToContent="WidthAndHeight"
        mc:Ignorable="d">
    <Window.DataContext>
        <demo:SettingsVm />
    </Window.DataContext>
    <StackPanel>
        <Button Content="lose focus" />

        <propertyGrid:SettingsControl DataContext="{Binding EditableCopy}" OldDataContext="{Binding DataContext.LastSaved, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
            <propertyGrid:StringSettingControl Header="string" Value="{Binding StringValue}" />
            <propertyGrid:StringSettingControl Header="propertychanged" Value="{Binding StringValue, UpdateSourceTrigger=PropertyChanged}" />
            <propertyGrid:StringSettingControl Header="readonly string" Value="{Binding StringValue}" />
        </propertyGrid:SettingsControl>
    </StackPanel>
</Window>
