<Window x:Class="Gu.Wpf.PropertyGrid.Demo.UiTestWindows.NestedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:propertyGrid="http://gu.se/PropertyGrid"
        Title="NestedWindow"
        SizeToContent="WidthAndHeight"
        mc:Ignorable="d">
    <StackPanel>
        <GroupBox Header="nested longer">
            <propertyGrid:SettingsControl>
                <propertyGrid:StringSettingControl Header="root"
                                                   Suffix="root suffix"
                                                   Value="root value" />

                <TextBlock>First nested below</TextBlock>
                <propertyGrid:SettingsControl Style="{StaticResource {x:Static propertyGrid:Keys.NestedSettingListStyle}}">
                    <propertyGrid:StringSettingControl Header="a"
                                                       Suffix="b"
                                                       Value="c" />
                </propertyGrid:SettingsControl>

                <TextBlock>Second nested below</TextBlock>
                <propertyGrid:SettingsControl Style="{StaticResource {x:Static propertyGrid:Keys.NestedSettingListStyle}}">
                    <propertyGrid:StringSettingControl Header="long header"
                                                       Suffix="really long suffix"
                                                       Value="really long value" />
                </propertyGrid:SettingsControl>
            </propertyGrid:SettingsControl>
        </GroupBox>

        <GroupBox Header="nested shorter">
            <propertyGrid:SettingsControl>
                <propertyGrid:StringSettingControl Header="root"
                                                   Suffix="suffix"
                                                   Value="value" />

                <TextBlock>First nested below</TextBlock>
                <propertyGrid:SettingsControl Style="{StaticResource {x:Static propertyGrid:Keys.NestedSettingListStyle}}">
                    <propertyGrid:StringSettingControl Header="a"
                                                       Suffix="ab"
                                                       Value="ac" />
                </propertyGrid:SettingsControl>

                <TextBlock>Second nested below</TextBlock>
                <propertyGrid:SettingsControl Style="{StaticResource {x:Static propertyGrid:Keys.NestedSettingListStyle}}">
                    <propertyGrid:StringSettingControl Header="b"
                                                       Suffix="bb"
                                                       Value="bc" />
                </propertyGrid:SettingsControl>
            </propertyGrid:SettingsControl>
        </GroupBox>
    </StackPanel>
</Window>
